GET /get/user - Получение профиля текущего пользователя.
POST /create/user - Создание нового пользователя.
{
  "name": "string"   // required
}

WS /ws - канал для всех событий от ядра. Подключается после инициализации UI.